#pragma once

#include <QColor>

namespace image_editor::ui {

inline constexpr QRgb kTransparencyCheckerLight = qRgb(205, 208, 214);
inline constexpr QRgb kTransparencyCheckerDark = qRgb(158, 162, 170);

} // namespace image_editor::ui
